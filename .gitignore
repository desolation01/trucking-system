# ── Dependencies ──
node_modules/

# ── Build output ──
dist/
dist-ssr/
*.tsbuildinfo

# ── Vite cache ──
.vite/

# ── Environment & secrets (DO NOT COMMIT) ──
.env
.env.local
.env.*
!.env.example
*.key
*.pem
*.crt
credentials.json
secrets.json

# ── Development logs ──
*.log
dev.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── Lock file (project uses npm; ignore pnpm/yarn alternates to avoid confusion) ──
pnpm-lock.yaml
yarn.lock

# ── IDE / editor ──
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# ── OS files ──
Thumbs.db
Desktop.ini

# ── AI / planning / agent files (not source code) ──
*.plan.md
implementation-plan.md
PRD.md
CLAUDE.md
AGENTS.md
AI_INSTRUCTIONS.md
.cursorrules
.windsurfrules
plans/
.ai/
.hermes/

# ── Misc local overrides ──
*.local

# ── Supabase CLI local state (contains project ref / pooler URL) ──
supabase/.temp/

# ── Security audit reports (document live exploit paths — never publish) ──
SECURITY-AUDIT.md
*-audit.md
*Audit.md
